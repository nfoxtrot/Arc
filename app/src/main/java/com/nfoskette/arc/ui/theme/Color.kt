package com.nfoskette.arc.ui.theme

import androidx.compose.ui.graphics.Color

// ARC design system color tokens (docs/DESIGN.md ยง3) — confirmed light/dark palette.
// These are the app's brand identity colors, used as-is rather than derived from
// Material You dynamic color, so the ARC brand looks the same across devices.

// Light mode
val LightBg = Color(0xFFFBF9F5)
val LightBgAlt = Color(0xFFF3EFE8)
val LightInk = Color(0xFF1E2023)
val LightInkSoft = Color(0xFF5B5D63)
val LightAccent = Color(0xFFE8829E)
val LightAccentSoft = Color(0x24E8829E) // rgba(232,130,158,0.14)
val LightLine = Color(0x1A1E2023) // rgba(30,32,35,0.10)
val LightCard = Color(0xFFFFFFFF)

// Dark mode
val DarkBg = Color(0xFF1B1D22) // gunmetal
val DarkBgAlt = Color(0xFF23262B)
val DarkInk = Color(0xFFECEDEE)
val DarkInkSoft = Color(0xFF9CA0A8)
val DarkAccent = Color(0xFF6E8FC2)
val DarkAccentSoft = Color(0x296E8FC2) // rgba(110,143,194,0.16)
val DarkLine = Color(0x1AECEDEE) // rgba(236,237,238,0.10)
val DarkCard = Color(0xFF23262B)
